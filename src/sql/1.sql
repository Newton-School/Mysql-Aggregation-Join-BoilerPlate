-- Query all the  flights with fields names origin_id, destination_id, and duration field and sort them by duration
SELECT origin as origin_id, destination as destination_id, duration FROM flights ORDER By duration;